
<nav>
    <div class="nav-wrapper grey darken-2">
        <a href="#!" class="brand-logo">State Teleporter</a>
    </div>
</nav>
<!-- Dynamically Render Country Tiles -->
<main id="tilesContent">
    {{#each states}}
    <div class="card grey darken-2">
        <div class="card-content white-text">
            <div>{{this.stateName}}</div>
            <div class="imgDiv" style="background-image: url('{{this.stateImageURL}}')"></div>
            <div id="{{this.id}}" class="plusMinusIcon"><a class="btn btn-floating waves-effect waves-light"><i class="material-icons">add</i></a>
            </div>
        </div>
        {{!--
        <div class="card-action">
            <a id="modelbtn" class="waves-effect waves-light btn" href="#modal1">Find Travel Buddy</a>
        </div> --}}
    </div>
    {{/each}}
</main>
<script src='http://code.jquery.com/jquery-2.1.3.min.js'></script>
<script type="text/javascript">
console.log('ckickers')
$(document).on('click', '.material-icons', function() {
    console.log($(this))
    var countryId = $(this)[0].attributes[1].value;
    var route = "/add/state/1/" + stateId;
    console.log(stateId);
    // console.log('cklicked')
    $.ajax({
            url: route,
            type: 'GET',
        })
        .done(function(res) {
            var results = res.data;
        })
        .fail(function() {
            console.log("error");
        })
        .always(function() {
            console.log("complete");
        });
})
</script>
